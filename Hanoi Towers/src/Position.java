// Utilzing ENUM's for the positions LEFT MIDDLE RIGHT, and OTHER
public enum Position {
    /**
     * These are my assoicated poisitons that my tower will have
     */
    LEFT, MIDDLE, RIGHT, OTHER;
}
